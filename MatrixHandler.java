public class MatrixHandler {

    //matrices should look like this {{}}
    public static Vertex vertexMultiplication(Vertex vertex, double[][] matrix, Camera camera){
        Vertex result = new Vertex();


        return result;
    }

}
